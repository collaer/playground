ortmtApp.ManageEditsTool = function(opt_options) {  var options = opt_options || {};  var this_ = this;  var button = document.createElement('button');  button.innerHTML = '<span class="glyphicon glyphicon-export"></span>';  $(button).attr({ "title":'Valider et exporter les données',"data-toggle" : 'tooltip', 'data-loading-text':".."});    var handleClick = function(e) {	var $btn = $(this).button('loading');	var geojson = ol.format.GeoJSON.prototype.writeFeatures(vectorLayer.getSource().getFeatures());	$.ajax({       url : 'brol.php',       type : 'POST',        dataType : 'html',	   data : 'Jason=' + encodeURIComponent(geojson),	   success : function(code_html, statut) {	     ortmtApp.alertTool.show({msg : 'Le plan a été mis à jour',level:'danger'});		 $btn.button('reset');		 this_.getMap().removeLayer(ortmapguide);		 this_.getMap().render();		 this_.getMap().addLayer(ortmapguide);		 this_.getMap().render();	   }    });  };  button.addEventListener('click', handleClick, false);  button.addEventListener('touchstart', handleClick, false);  var element = document.createElement('div');  element.className = 'edits-tool ol-unselectable ol-control';  element.appendChild(button);  ol.control.Control.call(this, {    element: element,    target: options.target  });};ol.inherits(ortmtApp.ManageEditsTool, ol.control.Control);